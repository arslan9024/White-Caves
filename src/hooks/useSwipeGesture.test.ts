import { describe, it, expect } from 'vitest';
import * as HookModule from './useSwipeGesture';

describe('useSwipeGesture (Hook)', () => {
  it('exports hook module correctly', () => {
    expect(HookModule).toBeDefined();
  });
});
