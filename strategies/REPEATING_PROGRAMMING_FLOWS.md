# 🔱 Strategic Repeating Programming Flows

This ledger contains our reusable, token‑optimized automation scripts. Use them in sequence to develop our Dubai real‑estate platform safely on our laptop without wasting premium credits.

---

### Flow 1: Global Gap Audit & Document Upgrade

- **When to use:** Use this at the start of a new sprint week to find out exactly what features or files are missing from your project structure.
- **Target Engine:** Run this inside **Antigravity IDE** (Free Version) to save context tokens.

```markdown
@workspace You are operating as AEGIS under the absolute authority of the `strategies/` directory to prevent premium token wastage.

Task: Perform a full‑repo architectural gap sweep using free worker threads. Cross‑examine all backend Prisma schemas against our files in `/src` and `/server`. Identify missing edge cases, unmapped endpoints, and unfinished role configurations for the 'Lion' user profile.

Do not modify any application code. Write your findings as a step‑by‑step checklist inside `plans/PENDING_TASKS_ONLY.md`. Output only the summary completion string: "[AEGIS WORKSPACE AUDIT PASS COMPLETE]".
```

---

### Flow 2: Automated Local Runtime Recovery (`npm run dev` Crash Fix)

- **When to use:** Use this whenever your local development environment crashes, experiences broken imports, or fails to boot.
- **Target Engine:** Run this inside **Antigravity IDE** or **VS Code Copilot Edits** (Autopilot Mode Active).

```markdown
@workspace Act as the Lead AEGIS Debugging Specialist. Execute the local laptop development terminal trigger: `npm run dev`.

Capture the raw console crash dump trace natively. Identify the exact file path and line number causing the failure. Following our `strategies/CREDIT_PRESERVATION_LAW.md` isolation rules, surgically patch the target file. If the crash is tied to a broken live database connection, instantly inject a high‑fidelity local mock data array to ensure a successful offline boot sequence. Iterate this loop until the local terminal outputs stable listening address channels without throwing errors.
```

---

### Flow 3: Surgical Modular Refactoring & Feature Implementation

- **When to use:** Use this when you are ready to write code, refactor a large file (like your dashboard monolith), or build out missing endpoint logic.
- **Target Engine:** Run this inside **VS Code Copilot Edits** (Surgical Token Allocation Mode).

```markdown
@workspace Act as the AEGIS Hard‑Execution Machine. Your target is to refactor or build the single, assigned component or endpoint file path.

You are strictly forbidden from reading or scanning files outside this active task boundary to prevent context token inflation. Integrate reusable blocks from our `DashboardComponents.tsx` library and use memoized data mappings from `useDashboardMetrics.ts`. Deliver 100% complete, production‑ready, TypeScript‑strict implementations. Do not leave placeholder text or empty comments behind. Save files to disk immediately.
```

---

### Flow 4: Local Compilation Verification (0‑Token Quality Gate)

- **When to use:** Use this immediately after writing or editing code files to fix minor compilation syntax bugs before pushing.
- **Target Engine:** Run this inside **Antigravity IDE** (Free Worker Mode).

```markdown
@workspace Execute our local laptop workspace verification task: `npm run build`.

If the local machine compiler returns TypeScript syntax errors, type mismatches, or import path flags, intercept the console trace natively. Task free worker threads to read the error text, isolate the source lines, and fix the files. Do not use premium reasoning tokens for basic syntax validation. Iterate until local compilation passes cleanly with exit code 0.
```

---

### Flow 5: Remote Sync, Git Merge, and Tracking Sign‑Off

- **When to use:** Use this only after your local environment is completely stable to safely push your updated code to your remote GitHub branch.
- **Target Engine:** Run this inside **Antigravity IDE** or **VS Code Terminal Automation**.

```markdown
@workspace Execute our secure automated remote sync pipeline:

1. git checkout develop
2. git add . && git commit -m "aegis/loop: completed target implementation turn | passed compilation"
3. git checkout main && git pull origin main --rebase
4. git merge develop (Auto‑resolve diff blocks by prioritizing our luxury design tokens and explicit schemas)
5. git push origin main to update our hosting layer and trigger Vercel automation deployment hooks.

Once complete, switch back to develop, update `DAILY_MILESTONE_TRACKER.md` under "Daily Entry", clear completed items from `plans/PENDING_TASKS_ONLY.md`, and stand by for the next turn.
```

---

### 💡 Strategy For Moving Forward

By keeping these 5 templates saved as an immutable strategy file on your laptop, you can move systematically through your project features without letting the AI wander away or drain your resources.

---
